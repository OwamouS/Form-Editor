import { SchemaTree } from "components/Configuration/appConfigurationM/types";
import React from "react";

type Props = {
  items: SchemaTree;
};

function ChildrenList({ items }: Props) {
  return <div>{}</div>;
}

export default ChildrenList;
